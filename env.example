# Copy this file to .env for local development.
# These variables are used by the contact API at src/pages/api/contact.ts

# SMTP server settings
# Example (Mailtrap): smtp.mailtrap.io, port 587
# Example (Gmail - not recommended for production): smtp.gmail.com, port 465
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Where to deliver contact form emails.
# Defaults to SMTP_USER if not set.
CONTACT_TO_EMAIL=your_destination@example.com

# Astro notes:
# - These variables are used server-side only. Do NOT prefix with PUBLIC_.
# - In production (e.g., Vercel), set the same variable names in the dashboard.
